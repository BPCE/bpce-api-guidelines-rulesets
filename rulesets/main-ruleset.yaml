extends:
  # These 2 includes are done in oas-ruleset but must be redone here to deactivate oas2&3 rules
  - [spectral:oas2, off] 
  - [spectral:oas3, off]
  - oas-ruleset.yaml
  - info-ruleset.yaml
  - http-methods-ruleset.yaml
rules:
  oas2-schema: true
  oas3-schema: true